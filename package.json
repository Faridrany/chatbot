{
  "name": "chatbot-pengaduan-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"BACKEND,CHATBOT,FRONTEND\" --prefix-colors \"green,yellow,cyan\" \"npm run start --prefix backend\" \"npm run start --prefix chatbot\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
